version: '3'
services:
  kinkhorn:
    stdin_open: true
    build:
      context: .
      dockerfile: dockerfile
    ports:
        - '3000:3000'
    volumes:
        - /app/node_modules
        - .:/app